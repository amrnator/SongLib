package Model;
import java.io.*;

public class FileRead {
	//constructor
	public static SongList createSongList (String fileName){
		SongList list = new SongList();
		try{
			//read from file
			FileReader x = new FileReader(fileName);
			//add buffer for safety reasons
			BufferedReader inputFile = new BufferedReader(x);
			
		}catch(FileNotFoundException ex){
			
		}
		return list;
	} 
}
